--- a/net/minecraft/world/level/biome/BiomeManager.java
+++ b/net/minecraft/world/level/biome/BiomeManager.java
@@ -14,8 +_,26 @@
     private static final int ZOOM_MASK = 3;
     private final BiomeManager.NoiseBiomeSource noiseBiomeSource;
     private final long biomeZoomSeed;
+    // Gale start - Cache - BiomeManager.getBiome()
+    private final long[] biomeCacheKeys;
+    private final Holder<Biome>[] biomeCacheValues;
+    // Gale end - Cache - BiomeManager.getBiome()
 
     public BiomeManager(final BiomeManager.NoiseBiomeSource noiseBiomeSource, final long seed) {
+        // Gale start - Cache - BiomeManager.getBiome()
+        this(noiseBiomeSource, seed, false);
+    }
+
+    public BiomeManager(final BiomeManager.NoiseBiomeSource noiseBiomeSource, final long seed, boolean initializeCache) {
+        if (initializeCache) {
+            this.biomeCacheKeys = new long[1 << 18];
+            java.util.Arrays.fill(this.biomeCacheKeys, Long.MIN_VALUE);
+            this.biomeCacheValues = new Holder[1 << 18];
+        } else {
+            this.biomeCacheKeys = null;
+            this.biomeCacheValues = null;
+        }
+        // Gale end - Cache - BiomeManager.getBiome()
         this.noiseBiomeSource = noiseBiomeSource;
         this.biomeZoomSeed = seed;
     }
@@ -35,6 +_,34 @@
         int parentX = absX >> 2;
         int parentY = absY >> 2;
         int parentZ = absZ >> 2;
+        // Gale start - Cache - BiomeManager.getBiome()
+        return this.getBiome(absX, absY, absZ, parentX, parentY, parentZ);
+    }
+
+    public Holder<Biome> getBiomeCached(final BlockPos pos) {
+        int absX = pos.getX() - 2;
+        int absY = pos.getY() - 2;
+        int absZ = pos.getZ() - 2;
+        int parentX = absX >> 2;
+        int parentY = absY >> 2;
+        int parentZ = absZ >> 2;
+
+        // Look up in cache
+        long cacheKey = BlockPos.asLong(parentX, parentY, parentZ);
+        int cacheIndex = ((int) it.unimi.dsi.fastutil.HashCommon.mix(cacheKey)) & ((1 << 18) - 1);
+        if (this.biomeCacheKeys[cacheIndex] == cacheKey) {
+            return this.biomeCacheValues[cacheIndex];
+        }
+
+        // Compute and store in cache
+        Holder<Biome> computedBiome = this.getBiome(absX, absY, absZ, parentX, parentY, parentZ);
+        this.biomeCacheKeys[cacheIndex] = cacheKey;
+        this.biomeCacheValues[cacheIndex] = computedBiome;
+        return computedBiome;
+    }
+
+    public Holder<Biome> getBiome(final int absX, final int absY, final int absZ, final int parentX, final int parentY, final int parentZ) {
+        // Gale end - Cache - BiomeManager.getBiome()
         double fractX = (absX & 3) / 4.0;
         double fractY = (absY & 3) / 4.0;
         double fractZ = (absZ & 3) / 4.0;
@@ -101,7 +_,20 @@
     }
 
     private static double getFiddle(final long rval) {
-        return (double)(((rval >> 24) & (1024 - 1)) - (1024/2)) * (0.9 / 1024.0); // Paper - avoid floorMod, fp division, and fp subtraction
+        // Gale start - Pre-compute - BiomeManager.getFiddle()
+        return PRECOMPUTED_FIDDLE_TABLE[(int) (rval >>> 24) & 1023];
+    }
+
+    private static final double[] PRECOMPUTED_FIDDLE_TABLE = new double[1024];
+    static {
+        for (int i = 0; i < 1024; i++) {
+            PRECOMPUTED_FIDDLE_TABLE[i] = precomputeFiddle(i);
+        }
+    }
+
+    private static double precomputeFiddle(final int i) {
+        // Gale end - Pre-compute - BiomeManager.getFiddle()
+        return (double)(i - (1024/2)) * (0.9 / 1024.0); // Paper - avoid floorMod, fp division, and fp subtraction
     }
 
     public interface NoiseBiomeSource {
