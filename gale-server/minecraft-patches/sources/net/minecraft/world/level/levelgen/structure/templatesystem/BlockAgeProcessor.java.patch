--- a/net/minecraft/world/level/levelgen/structure/templatesystem/BlockAgeProcessor.java
+++ b/net/minecraft/world/level/levelgen/structure/templatesystem/BlockAgeProcessor.java
@@ -51,7 +_,7 @@
             newState = this.maybeReplaceStairs(state, random);
         } else if (state.is(BlockTags.SLABS)) {
             newState = this.maybeReplaceSlab(state, random);
-        } else if (state.is(BlockTags.WALLS)) {
+        } else if (state.isAny(org.dreeam.leaf.util.BlockMasks.WALLS_TAG)) { // Gale - Pre-compute - Common BlockState predicates - Apply
             newState = this.maybeReplaceWall(state, random);
         } else if (state.is(Blocks.OBSIDIAN)) {
             newState = this.maybeReplaceObsidian(random);
