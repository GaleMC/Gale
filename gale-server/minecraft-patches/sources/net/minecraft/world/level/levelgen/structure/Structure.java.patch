--- a/net/minecraft/world/level/levelgen/structure/Structure.java
+++ b/net/minecraft/world/level/levelgen/structure/Structure.java
@@ -43,7 +_,17 @@
 import net.minecraft.world.level.levelgen.structure.pieces.StructurePiecesBuilder;
 import net.minecraft.world.level.levelgen.structure.templatesystem.StructureTemplateManager;
 
-public abstract class Structure {
+public abstract class Structure extends org.galemc.gale.registry.valuebasedmap.FieldRegistryIdAware { // Gale - Registry values can be id-aware - Make aware
+    // Gale start - Optimize maps with registry values as keys
+    private static java.util.function.@org.jspecify.annotations.Nullable Supplier<org.galemc.gale.registry.valuebasedmap.RegistryIdAware2ObjectArrayMap<Structure, ?>> registryIdAwareMapSupplier;
+    public static <V> org.galemc.gale.registry.valuebasedmap.RegistryIdAware2ObjectArrayMap<Structure, V> createRegistryIdAwareMap() {
+        if (registryIdAwareMapSupplier == null) {
+            Structure[] possibleKeys = net.minecraft.server.MinecraftServer.getServer().registryAccess().lookupOrThrow(Registries.STRUCTURE).stream().toArray(Structure[]::new);
+            registryIdAwareMapSupplier = () -> new org.galemc.gale.registry.valuebasedmap.RegistryIdAware2ObjectArrayMap<>(possibleKeys);
+        }
+        return (org.galemc.gale.registry.valuebasedmap.RegistryIdAware2ObjectArrayMap) registryIdAwareMapSupplier.get();
+    }
+    // Gale end - Optimize maps with registry values as keys
     public static final Codec<Structure> DIRECT_CODEC = BuiltInRegistries.STRUCTURE_TYPE.byNameCodec().dispatch(Structure::type, StructureType::codec);
     public static final Codec<Holder<Structure>> CODEC = RegistryFileCodec.create(Registries.STRUCTURE, DIRECT_CODEC);
     protected final Structure.StructureSettings settings;
