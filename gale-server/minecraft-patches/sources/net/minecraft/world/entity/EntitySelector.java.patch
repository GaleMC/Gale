--- a/net/minecraft/world/entity/EntitySelector.java
+++ b/net/minecraft/world/entity/EntitySelector.java
@@ -30,6 +_,13 @@
     // Paper end - Ability to control player's insomnia and phantoms
     // Paper start - Affects Spawning API
     public static final Predicate<Entity> PLAYER_AFFECTS_SPAWNING = (entity) -> {
+        // Gale start - Event-driven - Player.affectsSpawningSelector
+        return entity instanceof Player player && player.gale$eventDriven_affectsSpawningSelector;
+    };
+
+    public static boolean gale$eventDriven_affectsSpawningSelector_compute(Player entity) {
+        if (((net.minecraft.server.level.ServerPlayer) entity).gameMode == null) return false; // ServerPlayer.gameMode will be null during LivingEntity constructor
+        // Gale end - Event-driven - Player.affectsSpawningSelector
         return !entity.isSpectator() && entity.isAlive() && entity instanceof Player player && player.affectsSpawning;
     };
     // Paper end - Affects Spawning API
@@ -47,9 +_,8 @@
         Team.CollisionRule ownCollisionRule = ownTeam == null ? Team.CollisionRule.ALWAYS : ownTeam.getCollisionRule();
         return ownCollisionRule == Team.CollisionRule.NEVER
             ? Predicates.alwaysFalse()
-            : NO_SPECTATORS.and(
-                input -> {
-                    if (!input.isPushable() || !input.canCollideWithBukkit(entity) || !entity.canCollideWithBukkit(input)) { // CraftBukkit - collidable API // Paper - Climbing should not bypass cramming gamerule
+            : input -> { // Gale - Do less work - EntitySelector.pushableBy()
+                    if (input.isSpectator() /*|| !pushedEntity.isPushable()*/ || !input.canCollideWithBukkit(entity) || !entity.canCollideWithBukkit(input)) { // CraftBukkit - collidable API // Paper - Climbing should not bypass cramming gamerule // Gale - Do less work - EntitySelector.pushableBy()
                         return false;
                     }
 
@@ -69,8 +_,7 @@
                     } else {
                         return false;
                     }
-                }
-            );
+                }; // Gale - Do less work - EntitySelector.pushableBy()
     }
 
     public static Predicate<Entity> notRiding(final Entity entity) {
