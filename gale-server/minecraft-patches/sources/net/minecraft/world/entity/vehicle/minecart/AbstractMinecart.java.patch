--- a/net/minecraft/world/entity/vehicle/minecart/AbstractMinecart.java
+++ b/net/minecraft/world/entity/vehicle/minecart/AbstractMinecart.java
@@ -258,7 +_,7 @@
     @Override
     protected float getBlockSpeedFactor() {
         BlockState blockState = this.level().getBlockState(this.blockPosition());
-        return blockState.is(BlockTags.RAILS) ? 1.0F : super.getBlockSpeedFactor();
+        return blockState.isAny(org.dreeam.leaf.util.BlockMasks.RAILS_TAG) ? 1.0F : super.getBlockSpeedFactor(); // Gale - Pre-compute - Common BlockState predicates - Apply
     }
 
     @Override
@@ -341,10 +_,10 @@
         int zt = Mth.floor(this.getZ());
         if (useExperimentalMovement(this.level())) {
             double y = this.getY() - 0.1 - 1.0E-5F;
-            if (this.level().getBlockState(BlockPos.containing(xt, y, zt)).is(BlockTags.RAILS)) {
+            if (this.level().getBlockState(BlockPos.containing(xt, y, zt)).isAny(org.dreeam.leaf.util.BlockMasks.RAILS_TAG)) { // Gale - Pre-compute - Common BlockState predicates - Apply
                 yt = Mth.floor(y);
             }
-        } else if (this.level().getBlockState(new BlockPos(xt, yt - 1, zt)).is(BlockTags.RAILS)) {
+        } else if (this.level().getBlockState(new BlockPos(xt, yt - 1, zt)).isAny(org.dreeam.leaf.util.BlockMasks.RAILS_TAG)) { // Gale - Pre-compute - Common BlockState predicates - Apply
             yt--;
         }
 
