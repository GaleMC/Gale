--- a/net/minecraft/world/entity/vehicle/minecart/OldMinecartBehavior.java
+++ b/net/minecraft/world/entity/vehicle/minecart/OldMinecartBehavior.java
@@ -267,7 +_,7 @@
         int xt = Mth.floor(x);
         int yt = Mth.floor(y);
         int zt = Mth.floor(z);
-        if (this.level().getBlockState(new BlockPos(xt, yt - 1, zt)).is(BlockTags.RAILS)) {
+        if (this.level().getBlockState(new BlockPos(xt, yt - 1, zt)).isAny(org.dreeam.leaf.util.BlockMasks.RAILS_TAG)) { // Gale - Pre-compute - Common BlockState predicates - Apply
             yt--;
         }
 
@@ -305,7 +_,7 @@
         int xt = Mth.floor(x);
         int yt = Mth.floor(y);
         int zt = Mth.floor(z);
-        if (this.level().getBlockState(new BlockPos(xt, yt - 1, zt)).is(BlockTags.RAILS)) {
+        if (this.level().getBlockState(new BlockPos(xt, yt - 1, zt)).isAny(org.dreeam.leaf.util.BlockMasks.RAILS_TAG)) { // Gale - Pre-compute - Common BlockState predicates - Apply
             yt--;
         }
 
