--- a/net/minecraft/world/entity/ai/behavior/JumpOnBed.java
+++ b/net/minecraft/world/entity/ai/behavior/JumpOnBed.java
@@ -101,7 +_,7 @@
     }
 
     private boolean isBed(final ServerLevel level, final BlockPos bodyPos) {
-        return level.getBlockState(bodyPos).is(BlockTags.BEDS);
+        return level.getBlockState(bodyPos).isAny(org.dreeam.leaf.util.BlockMasks.BEDS_TAG); // Gale - Pre-compute - Common BlockState predicates - Apply
     }
 
     private Optional<BlockPos> getNearestBed(final Mob body) {
