--- a/net/minecraft/world/entity/monster/cubemob/SulfurCube.java
+++ b/net/minecraft/world/entity/monster/cubemob/SulfurCube.java
@@ -96,7 +_,7 @@
     private int fuse = -1;
     private List<SulfurCubeArchetype.ContactDamage> contactDamages = new ArrayList<>();
     public static final EntityDataAccessor<Integer> MAX_FUSE = SynchedEntityData.defineId(SulfurCube.class, EntityDataSerializers.INT);
-    private static final EntityDataAccessor<Boolean> FROM_BUCKET = SynchedEntityData.defineId(SulfurCube.class, EntityDataSerializers.BOOLEAN);
+    private static final EntityDataAccessor<Boolean> FROM_BUCKET = SynchedEntityData.defineId(SulfurCube.class, EntityDataSerializers.BOOLEAN); // Gale - Event-driven - Mob.requiresCustomPersistence - TODO Watch for new set calls
     private static final boolean DEFAULT_FROM_BUCKET = false;
     private static final float HORIZONTAL_HIT_ANGLE_SCALE = 1.6F;
     private static final float VERTICAL_HIT_ANGLE_SCALE = 0.5F;
@@ -108,6 +_,7 @@
         super(type, level);
         this.lookControl = new SulfurCube.SulfurCubeLookControl();
         this.moveControl = new SulfurCube.SulfurCubeMobMoveControl<>(this);
+        this.gale$eventDriven_requiresCustomPersistence_update(); // Gale - Event-driven - Mob.requiresCustomPersistence
     }
 
     @Override
@@ -158,6 +_,7 @@
     @Override
     public void setFromBucket(final boolean fromBucket) {
         this.entityData.set(FROM_BUCKET, fromBucket);
+        this.gale$eventDriven_requiresCustomPersistence_update(); // Gale - Event-driven - Mob.requiresCustomPersistence
     }
 
     @Override
@@ -230,9 +_,41 @@
     }
 
     @Override
-    public boolean requiresCustomPersistence() {
-        return super.requiresCustomPersistence() || this.hasBodyItem() || this.fromBucket();
-    }
+    // Gale start - Event-driven - Mob.requiresCustomPersistence
+    public boolean gale$eventDriven_requiresCustomPersistence_compute() {
+        return super.gale$eventDriven_requiresCustomPersistence_compute() || this.hasBodyItem() || this.fromBucket();
+        // Gale end - Event-driven - Mob.requiresCustomPersistence
+    }
+
+    // Gale start - Event-driven - Mob.requiresCustomPersistence
+    @Override
+    protected net.minecraft.world.entity.EntityEquipment createEquipment() {
+        return new net.minecraft.world.entity.EntityEquipment() {
+
+            @Override
+            public net.minecraft.world.item.ItemStack set(final net.minecraft.world.entity.EquipmentSlot slot, final net.minecraft.world.item.ItemStack itemStack) {
+                net.minecraft.world.item.ItemStack toReturn = super.set(slot, itemStack);
+                if (slot == EquipmentSlot.BODY) {
+                    SulfurCube.this.gale$eventDriven_requiresCustomPersistence_update();
+                }
+                return toReturn;
+            }
+
+            @Override
+            public void setAll(final net.minecraft.world.entity.EntityEquipment equipment) {
+                super.setAll(equipment);
+                SulfurCube.this.gale$eventDriven_requiresCustomPersistence_update();
+            }
+
+            @Override
+            public void clear() {
+                super.clear();
+                SulfurCube.this.gale$eventDriven_requiresCustomPersistence_update();
+            }
+
+        };
+    }
+    // Gale end - Event-driven - Mob.requiresCustomPersistence
 
     @Override
     public boolean canBeLeashed() {
@@ -307,6 +_,7 @@
                         }
                     }
                     this.dead = true;
+                    this.gale$eventDriven_isAlive_update(); // Gale - Event-driven - LivingEntity.isAlive
                 };
                 // Paper end - Fire events for SulfurCube explosions
                 if (this.level() instanceof ServerLevel level) {
