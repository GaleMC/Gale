--- a/net/minecraft/server/commands/FillBiomeCommand.java
+++ b/net/minecraft/server/commands/FillBiomeCommand.java
@@ -171,6 +_,7 @@
                     region.maxZ()
                 )
             );
+            level.getBiomeManager().invalidateBiomeCache(); // Gale - Cache - BiomeManager.getBiome() - Invalidate cache on writes
             return Either.left(finalChangedCount);
         } else {
             return Either.right(ERROR_NO_BIOMES_SET.create());
