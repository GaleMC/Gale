--- a/net/minecraft/server/players/PlayerList.java
+++ b/net/minecraft/server/players/PlayerList.java
@@ -514,15 +_,18 @@
         // CraftBukkit start
         // this.broadcastAll(new ClientboundPlayerInfoRemovePacket(List.of(player.getUUID())));
         ClientboundPlayerInfoRemovePacket packet = new ClientboundPlayerInfoRemovePacket(List.of(player.getUUID()));
+        org.bukkit.craftbukkit.entity.CraftPlayer craftPlayer = player.getBukkitEntity(); // Gale - Intrusive values - CraftPlayer.invertedVisibilityEntities
         for (int i = 0; i < this.players.size(); i++) {
             ServerPlayer otherPlayer = this.players.get(i);
 
-            if (otherPlayer.getBukkitEntity().canSee(player.getBukkitEntity())) {
+            if (otherPlayer.getBukkitEntity().canSee(craftPlayer)) { // Gale - Intrusive values - CraftPlayer.invertedVisibilityEntities
                 otherPlayer.connection.send(packet);
             } else {
                 otherPlayer.getBukkitEntity().onEntityRemove(player);
             }
         }
+        craftPlayer.resetCanSee(); // Gale - Intrusive values - CraftPlayer.invertedVisibilityEntities
+        net.minecraft.server.network.ServerGamePacketListenerImpl.gale$USED_PACKED_INDICES.freeUsed(player.connection.gale$packedIndex); // Gale - Collections - ChunkMap.TrackedEntity.seenBy
         // This removes the scoreboard (and player reference) for the specific player in the manager
         this.cserver.getScoreboardManager().removePlayer(player.getBukkitEntity());
         // CraftBukkit end
