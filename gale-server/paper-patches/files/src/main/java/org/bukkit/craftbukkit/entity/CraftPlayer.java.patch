--- a/src/main/java/org/bukkit/craftbukkit/entity/CraftPlayer.java
+++ b/src/main/java/org/bukkit/craftbukkit/entity/CraftPlayer.java
@@ -235,7 +_,14 @@
     private long lastPlayed = 0;
     private boolean hasPlayedBefore = false;
     private final ConversationTracker conversationTracker = new ConversationTracker();
-    private final Map<UUID, Set<WeakReference<Plugin>>> invertedVisibilityEntities = new HashMap<>();
+    // Gale start - Intrusive values - CraftPlayer.invertedVisibilityEntities
+    public static boolean[] USED_PACKED_VISIBILITY_INVERSIONS_BIT_INDICES = new boolean[512];
+    public static final it.unimi.dsi.fastutil.objects.Object2IntOpenHashMap<UUID> occurrencesInInvertedVisibilityEntities = new it.unimi.dsi.fastutil.objects.Object2IntOpenHashMap<>();
+    public final Map<UUID, List<WeakReference<Plugin>>> invertedVisibilityEntities = new it.unimi.dsi.fastutil.objects.Object2ObjectOpenHashMap<>();
+    public final int packedVisibilityInversionsBitIndex;
+    public final int packedVisibilityInversionsWordIndex;
+    public final long packedVisibilityInversionsWordMask;
+    // Gale end - Intrusive values - CraftPlayer.invertedVisibilityEntities
     private final Set<UUID> unlistedEntities = new HashSet<>(); // Paper - Add Listing API for Player
     private static final WeakHashMap<Plugin, WeakReference<Plugin>> pluginWeakReferences = new WeakHashMap<>();
     private int hash = 0;
@@ -251,6 +_,23 @@
         super(server, entity);
 
         this.firstPlayed = System.currentTimeMillis();
+        // Gale start - Intrusive values - CraftPlayer.invertedVisibilityEntities
+        int i = 0;
+        while (true) {
+            if (i >= USED_PACKED_VISIBILITY_INVERSIONS_BIT_INDICES.length) {
+                USED_PACKED_VISIBILITY_INVERSIONS_BIT_INDICES = java.util.Arrays.copyOf(USED_PACKED_VISIBILITY_INVERSIONS_BIT_INDICES, USED_PACKED_VISIBILITY_INVERSIONS_BIT_INDICES.length + 512);
+                break;
+            }
+            if (!USED_PACKED_VISIBILITY_INVERSIONS_BIT_INDICES[i]) {
+                break;
+            }
+            i++;
+        }
+        USED_PACKED_VISIBILITY_INVERSIONS_BIT_INDICES[i] = true;
+        this.packedVisibilityInversionsBitIndex = i;
+        this.packedVisibilityInversionsWordIndex = i >> 6;
+        this.packedVisibilityInversionsWordMask = 1L << i;
+        // Gale end - Intrusive values - CraftPlayer.invertedVisibilityEntities
     }
 
     @Override
@@ -263,7 +_,11 @@
         // Long-term, this should just use the super equals... for now, check the UUID
         if (obj == this) return true;
         if (!(obj instanceof OfflinePlayer other)) return false;
-        return this.getUniqueId().equals(other.getUniqueId());
+        // Gale start - Local code optimization - Inline player UUID comparison
+        UUID uuid1 = this.getUniqueId();
+        UUID uuid2 = other.getUniqueId();
+        return uuid1.getMostSignificantBits() == uuid2.getMostSignificantBits() && uuid1.getLeastSignificantBits() == uuid2.getLeastSignificantBits();
+        // Gale end - Local code optimization - Inline player UUID comparison
     }
 
     @Override
@@ -1851,16 +_,23 @@
     }
 
     private boolean addInvertedVisibility(@Nullable Plugin plugin, org.bukkit.entity.Entity entity) {
-        Set<WeakReference<Plugin>> invertedPlugins = this.invertedVisibilityEntities.get(entity.getUniqueId());
+        // Gale start - Intrusive values - CraftPlayer.invertedVisibilityEntities
+        UUID uuid = entity.getUniqueId();
+        List<WeakReference<Plugin>> invertedPlugins = this.invertedVisibilityEntities.get(uuid);
+        ((CraftEntity) entity).setVisibilityInverted(this);
+        // Gale end - Intrusive values - CraftPlayer.invertedVisibilityEntities
         if (invertedPlugins != null) {
             // Some plugins are already inverting the entity. Just mark that this
             // plugin wants the entity inverted too and end.
             invertedPlugins.add(CraftPlayer.getPluginWeakReference(plugin));
             return false;
         }
-        invertedPlugins = new HashSet<>();
+        invertedPlugins = new ArrayList<>(2); // Gale - Intrusive values - CraftPlayer.invertedVisibilityEntities
         invertedPlugins.add(CraftPlayer.getPluginWeakReference(plugin));
-        this.invertedVisibilityEntities.put(entity.getUniqueId(), invertedPlugins);
+        // Gale start - Intrusive values - CraftPlayer.invertedVisibilityEntities
+        this.invertedVisibilityEntities.put(uuid, invertedPlugins);
+        occurrencesInInvertedVisibilityEntities.addTo(uuid, 1);
+        // Gale end - Intrusive values - CraftPlayer.invertedVisibilityEntities
 
         return true;
     }
@@ -1896,7 +_,13 @@
             return;
         }
 
-        if (this.invertedVisibilityEntities.remove(entity.getUniqueId()) == null) {
+        // Gale start - Intrusive values - CraftPlayer.invertedVisibilityEntities
+        UUID uuid = entity.getUniqueId();
+        if (this.invertedVisibilityEntities.remove(uuid) != null) {
+            ((CraftEntity) entity).unsetVisibilityInverted(this);
+            if (occurrencesInInvertedVisibilityEntities.addTo(uuid, -1) == 1) { occurrencesInInvertedVisibilityEntities.removeInt(uuid); }
+            // Gale end - Intrusive values - CraftPlayer.invertedVisibilityEntities
+        } else {
             this.untrackAndHideEntity(entity);
         }
     }
@@ -1933,15 +_,22 @@
     }
 
     private boolean removeInvertedVisibility(@Nullable Plugin plugin, org.bukkit.entity.Entity entity) {
-        Set<WeakReference<Plugin>> invertedPlugins = this.invertedVisibilityEntities.get(entity.getUniqueId());
+        // Gale start - Intrusive values - CraftPlayer.invertedVisibilityEntities
+        UUID uuid = entity.getUniqueId();
+        List<WeakReference<Plugin>> invertedPlugins = this.invertedVisibilityEntities.get(uuid);
+        // Gale end - Intrusive values - CraftPlayer.invertedVisibilityEntities
         if (invertedPlugins == null) {
             return false; // Entity isn't inverted
         }
-        invertedPlugins.remove(CraftPlayer.getPluginWeakReference(plugin));
+        while (invertedPlugins.remove(CraftPlayer.getPluginWeakReference(plugin))) {} // Gale - Intrusive values - CraftPlayer.invertedVisibilityEntities
         if (!invertedPlugins.isEmpty()) {
             return false; // Some other plugins still want the entity inverted
         }
-        this.invertedVisibilityEntities.remove(entity.getUniqueId());
+        // Gale start - Intrusive values - CraftPlayer.invertedVisibilityEntities
+        this.invertedVisibilityEntities.remove(uuid);
+        ((CraftEntity) entity).unsetVisibilityInverted(this);
+        if (occurrencesInInvertedVisibilityEntities.addTo(uuid, -1) == 1) { occurrencesInInvertedVisibilityEntities.removeInt(uuid); }
+        // Gale end - Intrusive values - CraftPlayer.invertedVisibilityEntities
 
         return true;
     }
@@ -1966,8 +_,14 @@
         }
 
         ChunkMap.TrackedEntity entry = tracker.entityMap.get(other.getId());
-        if (entry != null && !entry.seenBy.contains(this.getHandle().connection)) {
+        // Gale start - Collections - ChunkMap.TrackedEntity.seenBy
+        if (entry != null) {
+            ServerPlayer handle = this.getHandle();
+            ServerGamePacketListenerImpl connection = handle.connection;
+            if (!entry.seenByPackedArray.get(connection.packedSeenByWordIndex, connection.packedSeenByWordMask)) {
+            // Gale end - Collections - ChunkMap.TrackedEntity.seenBy
             entry.updatePlayer(this.getHandle());
+            } // Gale - Collections - ChunkMap.TrackedEntity.seenBy
         }
 
         this.server.getPluginManager().callEvent(new PlayerShowEntityEvent(this, entity));
@@ -2012,7 +_,13 @@
             return;
         }
 
-        if (this.invertedVisibilityEntities.remove(entity.getUniqueId()) == null) {
+        // Gale start - Intrusive values - CraftPlayer.invertedVisibilityEntities
+        UUID uuid = entity.getUniqueId();
+        if (this.invertedVisibilityEntities.remove(uuid) != null) {
+            ((CraftEntity) entity).unsetVisibilityInverted(this);
+            if (occurrencesInInvertedVisibilityEntities.addTo(uuid, -1) == 1) { occurrencesInInvertedVisibilityEntities.removeInt(uuid); }
+        } else {
+            // Gale end - Intrusive values - CraftPlayer.invertedVisibilityEntities
             this.trackAndShowEntity(entity);
         }
     }
@@ -2046,8 +_,28 @@
     // Paper end
 
     public void onEntityRemove(Entity entity) {
-        this.invertedVisibilityEntities.remove(entity.getUUID());
-    }
+        // Gale start - Intrusive values - CraftPlayer.invertedVisibilityEntities
+        UUID uuid = entity.getUUID();
+        if (this.invertedVisibilityEntities.remove(uuid) != null) {
+            entity.getBukkitEntity().unsetVisibilityInverted(this);
+            if (occurrencesInInvertedVisibilityEntities.addTo(uuid, -1) == 1) { occurrencesInInvertedVisibilityEntities.removeInt(uuid); }
+        }
+        // Gale end - Intrusive values - CraftPlayer.invertedVisibilityEntities
+    }
+
+    // Gale start - Intrusive values - CraftPlayer.invertedVisibilityEntities
+    public void resetCanSee() {
+        for (UUID uuid : this.invertedVisibilityEntities.keySet()) {
+            org.bukkit.entity.Entity entity = org.bukkit.Bukkit.getServer().getEntity(uuid);
+            if (entity != null) {
+                ((CraftEntity) entity).unsetVisibilityInverted(this);
+            }
+            if (occurrencesInInvertedVisibilityEntities.addTo(uuid, -1) == 1) { occurrencesInInvertedVisibilityEntities.removeInt(uuid); }
+        }
+        this.invertedVisibilityEntities.clear();
+        USED_PACKED_VISIBILITY_INVERSIONS_BIT_INDICES[this.packedVisibilityInversionsBitIndex] = false;
+    }
+    // Gale end - Intrusive values - CraftPlayer.invertedVisibilityEntities
 
     @Override
     public boolean canSee(Player player) {
@@ -2056,8 +_,14 @@
 
     @Override
     public boolean canSee(org.bukkit.entity.Entity entity) {
-        return this.equals(entity) || entity.isVisibleByDefault() ^ this.invertedVisibilityEntities.containsKey(entity.getUniqueId()); // SPIGOT-7312: Can always see self
-    }
+        return this.equals(entity) || entity.isVisibleByDefault() ^ (!this.invertedVisibilityEntities.isEmpty() && ((CraftEntity) entity).isVisibilityInvertedFor(this)); // SPIGOT-7312: Can always see self // Gale - Intrusive values - CraftPlayer.invertedVisibilityEntities
+    }
+
+    // Gale start - Do less work - Skip self check in CraftPlayer.canSee() if called from ChunkMap.updatePlayer()
+    public boolean canSeeSkipSelfCheck(org.bukkit.entity.Entity entity) {
+        return entity.isVisibleByDefault() ^ (!this.invertedVisibilityEntities.isEmpty() && ((CraftEntity) entity).isVisibilityInvertedFor(this)); // SPIGOT-7312: Can always see self // Gale - Intrusive values - CraftPlayer.invertedVisibilityEntities
+    }
+    // Gale end - Do less work - Skip self check in CraftPlayer.canSee() if called from ChunkMap.updatePlayer()
 
     public boolean canSeePlayer(UUID uuid) {
         org.bukkit.entity.Entity entity = this.getServer().getPlayer(uuid);
@@ -2581,7 +_,12 @@
                 this.sendHealthUpdate();
             }
         }
-        this.getHandle().getEntityData().set(net.minecraft.world.entity.LivingEntity.DATA_HEALTH_ID, (float) this.getScaledHealth());
+        // Gale start - Event-driven - LivingEntity.isAlive
+        float newHealth = this.getScaledHealth();
+        ServerPlayer handle = this.getHandle();
+        handle.getEntityData().set(net.minecraft.world.entity.LivingEntity.DATA_HEALTH_ID, newHealth);
+        handle.gale$eventDriven_isAlive_updateFor_health(newHealth);
+        // Gale end - Event-driven - LivingEntity.isAlive
 
         this.getHandle().maxHealthCache = this.getMaxHealth();
     }
@@ -2714,7 +_,14 @@
 
     // Paper start
     public void setAffectsSpawning(boolean affects) {
-        this.getHandle().affectsSpawning = affects;
+        // Gale start - Event-driven - Player.affectsSpawningSelector
+        ServerPlayer handle = this.getHandle();
+        boolean currentAffects = handle.affectsSpawning;
+        if (currentAffects != affects) {
+            handle.affectsSpawning = affects;
+            handle.gale$eventDriven_affectsSpawningSelector_update();
+        }
+        // Gale end - Event-driven - Player.affectsSpawningSelector
     }
 
     @Override
